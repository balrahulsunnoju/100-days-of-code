# AIDLC Starter Kit — Release Capability Delta

| Field | Value |
|-------|-------|
| From | master (22 prompts, 1 skill, 3 monolithic guides, v1.5.0) |
| To | v2.1.1 (33 prompts, 11 skills, 14 topic guides, 3 maintainer skills) |
| Generated | 2026-07-20 (delta update 6) |
| Generated by | `/i2a-manage-spec delta` |
| Method | Content comparison of original master baseline vs v2.1.0 |

---

## Part 1 — Capability Baseline (Master)

### EM Capabilities on Master

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Workspace bootstrap | `setup-workspace` | Create docs repo structure, install BMAD, configure workspace paths and i2a-config.yml |
| 2 | Quick install/upgrade | `quick-setup` | Lightweight BMAD install or upgrade; copy prompts, skills, config template |
| 3 | Pre-commit hooks | `install-hooks` | Install Gitleaks and conventional-commit hooks in all worker repos |
| 4 | Project initiation | `initiate-project` | Create protected project/* + planning/* branches, fetch GitHub issue, generate change brief |
| 5 | Issue initiation | `initiate-issue` | Start work from GitHub issue (feature/hotfix/project/bug types) |
| 6 | Reference sync | `reference-sync` | Pull common-services documentation from external GitHub repos into local KB |
| 7 | Context switching | `switch-issue` | Switch docs + worker repos to a different issue's branches |
| 8 | Conventional commits | `commit` | Multi-repo commit with branch validation and sensitivity checks |
| 9 | Pull request creation | `create-pull-request` | Multi-repo PR creation with auto-targeted branches |
| 10 | Post-deployment sync | `post-deployment-docs-sync` | KB regeneration after production release |
| 11 | KB synchronization | `update-context-docs` | Scan worker repos and regenerate context docs on release/project branches |

### ADE Capabilities on Master

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Workspace bootstrap | `setup-workspace` | Same as EM but with ADE-specific persona detection |
| 2 | Quick install/upgrade | `quick-setup` | Same as EM |
| 3 | Issue initiation | `initiate-issue` | Start work from assigned GitHub issue |
| 4 | Story discovery | `show-available-stories` | Parse sprint-status.yaml, scan remote branches, categorize stories |
| 5 | Repo preparation | `prepare-repos` | Create integration/epic/dev branches in affected worker repos |
| 6 | Context switching | `switch-issue` | Switch workspace to different issue |
| 7 | Conventional commits | `commit` | Multi-repo conventional commit workflow |
| 8 | CI pre-check | `pre-check-pull-request` | Trigger and monitor CI pipeline on dev branch |
| 9 | Pull request creation | `create-pull-request` | Create PRs targeting correct integration/epic branch |

### Test Management Capabilities on Master

| # | Capability | Details |
|---|-----------|---------|
| 1 | Unit test framework setup | `setup-unit-tests` — scaffold JUnit/Jest/Vitest/pytest per repo |
| 2 | API test framework setup | `setup-api-tests` — scaffold Insomnia collections, environments per service |
| 3 | Functional test framework setup | `setup-functional-tests` — scaffold Playwright framework in UI repo |
| 4 | Unit test generation | `generate-unit-tests` — generate tests targeting coverage threshold |
| 5 | API test generation | `generate-api-tests` — discover endpoints, generate Insomnia collections |
| 6 | Functional test generation | `generate-functional-tests` — generate Playwright specs from flow descriptors |
| 7 | Runbook validation | `validate-runbook-context` — cross-reference runbook vs KB (10 categories) |
| 8 | Test context validation | `validate-test-context` — cross-reference test cases vs business rules |

### Documentation on Master

3 monolithic guides in `agentic-user-guide/`:
- `engineering-manager-user-guide.md` — single-file EM reference
- `agentic-delivery-engineer-guide.md` — single-file ADE reference
- `test-management-user-guide.md` — single-file test management reference

No topic-based architecture, no role-based reading maps, no ACE documentation, no specifications, no prompt reference, no MCP guide.

### Quality Infrastructure on Master

8 test suites (inventory, cross-refs, prompts, guides, versions, workflow, quality, simulation) with `test-bmad-upgrade.js` as a standalone BMAD version check. Test-all.js header references 22 prompts and 3 guides. Simulation suite existed with deterministic decision rules.

---

## Part 2 — New Capabilities (Current Branch)

### Sprint Management — 5 New Capabilities

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Sprint dashboard generation | `generate-dashboard` (NEW) | Generate live HTML sprint dashboard from planning artifacts with 5-second auto-refresh from YAML; delegates to `sprint-dashboard` skill |
| 2 | Sprint metrics updates | `update-metrics` (NEW) | Update sprint-status.yaml after story status changes; validates state machine transitions; calculates Harvey ball quality metrics (0-4 scale) |
| 3 | Blocker management | `manage-blockers` (NEW) | Add, resolve, or update blockers on stories; includes impact level, action needed, resolution tracking |
| 4 | Metrics reporting | `metrics-report` (NEW) | Generate markdown sprint-metrics-report.md with velocity, completion rate, blocker count, cycle time |
| 5 | Mid-project course correction | `project-course-correction` (NEW) | Accept change requests from multiple sources (issue comment, sub-issue, doc, URL, inline); generate CR brief; delegate impact analysis to `/bmad-correct-course`; apply approved changes to specs, stories, kanban |

### Sprint Planning — 1 New Capability

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Kanban planning orchestrator | `project-kanban-planning` prompt (NEW) | Prompt wrapper for existing skill; validates prerequisites (PRD, architecture), auto-fills gaps via BMAD skills, generates kanban plan + dashboard + metrics |

### Test Execution — 2 New Capabilities

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Test execution and reporting | `run-tests` (NEW) | Discover repos with test infrastructure; execute across unit/API/functional layers; generate consolidated reports (HTML, markdown, JSON); classify results as pass/fail/skip/data-issue/infra |
| 2 | Test data catalog generation | `setup-testdata` (NEW) | Discover API chains, identity fields, UI screens; generate test-data-catalog.yaml with pools, stubs, cross-service configs; auto-add to .gitignore |

### Context Switching — 1 New Capability

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Role-aware context switching | `switch` (NEW, replaces `switch-issue`) | Adds explicit EM/ADE role parameter; EM switches to planning/* branches, ADE to dev/* branches; direct spec-path argument bypasses interactive selection |

### Custom Skills — 11 New Skills

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Sprint dashboard skill | `tdgs-aidlc-sprint-dashboard` (NEW, 16 files) | Parse epics + kanban + sprint-status → generate standalone HTML dashboard with KPIs, Harvey balls, blocker tracking, critical path visualization; includes sub-workflows for update-metrics, manage-blockers, metrics-report |
| 2 | API test framework skill | `tdgs-aidlc-setup-api-tests` (NEW, 9 files) | Scaffold per-service api-tests/ with Insomnia collections, test runner, report generator, collection linter (LINT-1..12), coverage auditor; ships pinned runner output contract for CI/CD |
| 3 | API test generation skill | `tdgs-aidlc-generate-api-tests` (NEW, 16 files) | Two-phase discovery workflow, pre-write contract (9 blocks), 25 guardrails (G1-G25), post-generation mechanical gate (20 checks), field derivation hierarchy (P0-P6), Insomnia collection generation by test category |
| 4 | Unit test framework skill | `tdgs-aidlc-setup-unit-tests` (NEW, 8 files) | Auto-detect technology stack (Java/Maven/Gradle, JS/TS with Jest/Vitest, Python/pytest, .NET/xUnit, Angular, Vue, Lambda); scaffold per-repo coverage tooling, `generate-report.js`, `coverage.json`, `TESTING.md`; 6 tool docs; hermetic by design (no catalog dependency) |
| 5 | Unit test generation skill | `tdgs-aidlc-generate-unit-tests` (NEW, 10 files) | KB + source discovery; pre-write contract (U1–U3); 8 guardrails (hermeticity, exception paths, exact assertions); per-module generation with coverage gates; 8 post-generation checks; supports JaCoCo/Jest/Vitest/pytest/Coverlet |
| 6 | Functional test framework skill | `tdgs-aidlc-setup-functional-tests` (NEW, 10 files) | Scaffold Playwright infrastructure in UI repos: `playwright.config.js`, page-object models, fixtures (`api-mock.js`, catalog fixture, flow-runner), component helpers (react-select, datepicker, wizard), flow descriptors; 8 tool docs; idempotent |
| 7 | Functional test generation skill | `tdgs-aidlc-generate-functional-tests` (NEW, 12 files) | Multi-phase discovery (flow descriptors, KB, source); pre-write contract (F1–F6); gap analysis; 14+ post-gen checks + Standards Audit Script; mock/real execution modes; flow × test-case × persona matrix; HTML/markdown/ledger reports |
| 8 | Test data catalog skill | `tdgs-aidlc-setup-testdata` (NEW, 10 files) | 5-step pipeline: project-context validation, multi-phase discovery (endpoints, API chains, screens, identity pools), interactive data collection with workbook import, catalog YAML generation with idempotent merge, dashboard script + JSON Schema generation; 13 guardrails (G1–G13); field derivation hierarchy (P0–P6); 20 hard rules; 8 tool docs |
| 9 | Help and reference skill | `tdgs-aidlc-help` (NEW, 4 files) | Read-only reference skill with four response modes: full catalog, targeted prompt/skill detail, goal-based command lookup, and workflow sequences; 2 tool docs (catalog-data.md, workflow-sequences.md); never modifies files |
| 10 | Ops runbook skill | `tdgs-aidlc-ops-runbook` (NEW, 12 files) | Dual-mode skill: Update mode surgically edits existing `.docx` or `.md` runbooks scoped to implementation plan version matrix; Create mode generates new `.md` runbook from Texas.gov template with Mermaid diagrams and optional UI screenshots; ships org-defaults, guardrails, diagram standards, section grounding rules, post-generation checks, and rendering scripts |

### Documentation — 2 New Capabilities

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Knowledge base generation | `generate-kb` (NEW) | Assemble and execute Document Project prompts; Apigee auto-detect (git/exports/false); assembles per-repo KB generation prompt for EM confirmation before delegating to `/bmad-document-project` |
| 2 | Ops runbook update/create | `ops-runbook` (NEW) | Dual-mode prompt: Update surgically edits existing `.docx`/`.md` runbooks; Create generates new `.md` from Texas.gov template; delegates to `tdgs-aidlc-ops-runbook` skill |

### Help and Reference — 1 New Capability

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Interactive command reference | `help` (NEW) | Read-only prompt surfacing the full AIDLC catalog without modifying files; four modes: (1) full at-a-glance catalog of all 33 prompts and 11 skills with model-complexity indicators, (2) targeted prompt/skill detail, (3) free-text goal mapping, (4) workflow sequences for 7 common scenarios; flexible name matching (prefix-optional, partial matches) |

### Topic-Based Documentation — 14 New Guides

| # | Guide | Who | What It Covers |
|---|-------|-----|---------------|
| 1 | `em-guide.md` | EM | Role entry point with reading map linking to all EM-relevant guides |
| 2 | `ade-guide.md` | ADE | Role entry point with reading map linking to all ADE-relevant guides |
| 3 | `setup.md` | Both | Prerequisites, workspace setup steps, update instructions, optional MCP extensions |
| 4 | `knowledge-base-generation.md` | EM | KB generation workflow, final structure, incremental repo additions |
| 5 | `mo-assignment.md` | EM | Issue types, feature/hotfix assignment, bug assignment, release responsibilities |
| 6 | `mo-workflow.md` | ADE | M&O Quick-Flow steps with embedded issue/PR template examples |
| 7 | `project-planning.md` | EM | Full BMAD project planning workflow (product brief through sprint dashboard) |
| 8 | `project-implementation.md` | ADE | Full BMAD project implementation steps (story pickup through PR) |
| 9 | `post-deployment.md` | EM | Post-deployment docs sync procedure |
| 10 | `test-management.md` | Both | Three-layer test pipeline, categorization, slicing, reports, operational rules |
| 11 | `reference.md` | Both | BMAD skills reference, branch naming, troubleshooting, quick reference card |
| 12 | `prompt-reference.md` | Both | Full command reference for all 33 prompts + 11 skills + BMAD commands |
| 13 | `mcp-setup-guide.md` | Both | GitHub MCP, Splunk MCP, database MCP (Oracle, MySQL, PostgreSQL, MongoDB) setup |
| 14 | `ops-runbook-update.md` | Both | Operational runbook update and create workflow guide |

### ACE (Agentic Capability Engineer) Tooling — 7 New Components

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Specification generation | `i2a-manage-spec/` skill | Generate/update/validate functional and technical specs from codebase scan; delta mode for training docs |
| 2 | Test suite management | `i2a-manage-test/` skill | Manage and maintain the starter kit's validation test suite |
| 3 | Adversarial review | `i2a-manage-review/` skill | 6-persona adversarial review (PM, BA, Dev, QA, Architect, UX) with fix-by-ID workflow |
| 4 | Dependency catalog | `catalog.md` | Complete dependency graph, upgrade impact matrix, config-consumer map, prompt workflow chains |
| 5 | Functional specification | `functional-specification.md` | Capability-area FR/NFR document (51 FRs, 9 NFRs) |
| 6 | Technical specification | `technical-specification.md` | Architecture contract — repo structure, patterns, test framework, extension guides |
| 7 | Project context | `project-context.md` | AI agent context file for ACE sessions |

### Infrastructure — 3 New Components

| # | Capability | Component | Details |
|---|-----------|-----------|---------|
| 1 | Repository restructure | `src/` layout | Prompts, skills, config moved from `docs-github-starter/` to `src/prompts/`, `src/i2a-skills/`, `src/i2a-config.yml` |
| 2 | Custom rules template | `src/templates/project-context-custom-rules.md` | Reusable template for workspace-generated project-context custom rules |
| 3 | User-controlled version override | `NEXT_VERSION` file | Optional semver override for the release pipeline; when populated with a version greater than the auto-incremented value, the release uses it; cleared automatically after each release commit |

---

## Part 3 — Enhanced Capabilities (Existed on Master, Changed Behavior)

### All 31 Prompts — YAML Frontmatter

| Aspect | Master | Current |
|--------|--------|---------|
| Mode declaration | Bare markdown (`.prompt.md` extension only); 4 prompts had ` ```prompt ``` ` code fence | All 33 prompts have YAML frontmatter with `mode: agent` and `description` fields |
| Trigger phrases | Not available | Each prompt's `description` frontmatter field provides a trigger phrase for GitHub Copilot discovery |

### install-hooks — Role Change

| Aspect | Master | Current |
|--------|--------|---------|
| Target role | ADE | All (EM + ADE) — both roles install pre-commit hooks |

### manage-blockers — Role Change

| Aspect | Master | Current |
|--------|--------|---------|
| Target role | Both (EM + ADE) | EM only — blocker management is an EM sprint-management responsibility |

### initiate-issue — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| GitHub MCP | Not required | Mandatory activation gate — BAIL if GitHub MCP not available |
| Bug type | Basic bug branch creation | Full bug brief generation with reproduction steps, linked parent project verification |
| Branch naming | Basic pattern | Max 100-char enforced, kebab-case slug from issue title |
| Quick-Dev vs Dev-Story | Not distinguished | Explicit guidance on which BMAD skill to invoke based on issue type |

### initiate-project — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| GitHub MCP | Not required | Mandatory activation gate |
| ADE team ID | Manual specification | Auto-discovery steps for ADE team ID from GitHub org |
| Branch protection | Not specified | CI guard workflow for project/* branch protection (only sprint-status.yaml and bug-brief-*.md allowed via direct push) |

### prepare-repos — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Worker repo mapping | Static config reference | Dynamic mapping guidance matching current config template shape |
| Bug branches | Basic support | Full 3-tier bug branch derivation (project-level, epic-level, story-level) |
| Collision prevention | Basic check | Explicit error identifying claiming user by remote dev/* branch detection |

### commit — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Repo discovery | Scan subdirectories for `.git` folders | Read `worker_repos` + `common_repos` from i2a-config.yml; symlinked common repos included; fallback to `.git` scan if no config |
| Sensitivity checks | Basic .env blocking | Expanded sensitive-file patterns (credentials, tokens, keys) |
| Branch validation | dev/* only | dev/*, planning/*, project/* (sprint-status.yaml only) |
| Protected branch list | master, main, release/*, feature/*, hotfix/* | master, release/*, feature/*, hotfix/* (`main` removed — not used in TDGS workflows) |
| Scope derivation | Manual | Auto-derived from current issue context via workspace config |

### create-pull-request — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Repo discovery | Scan subdirectories for `.git` folders | Read `worker_repos` + `common_repos` from i2a-config.yml; symlinked common repos included; fallback to `.git` scan if no config |
| Bug PR targeting | Not handled | Bug branch → parent branch (epic or project) based on naming convention |
| Planning PR | Not handled | planning/* → project/* targeting for EM planning artifacts |
| Copilot reviewer | Not mentioned | Auto-add Copilot as reviewer when available |
| GitHub issue comment | Not done | Auto-comment on GitHub issue after PR creation |

### pre-check-pull-request — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Config consumer | Not a config consumer | Now reads `worker_repos` + `common_repos` from i2a-config.yml for repo discovery |
| Repo discovery | Scan subdirectories for `.git` folders | i2a-config-driven with symlink support; fallback to `.git` scan if no config |

### switch — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Repo discovery | Scan subdirectories for `.git` folders | Read `worker_repos` + `common_repos` from i2a-config.yml; symlinked common repos always followed |

### setup-workspace — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Config preservation | Overwrites i2a-config.yml | Preserves existing `.github/i2a-config.yml` user configuration |
| kb_generation config | Not present | Generated i2a-config.yml templates include `kb_generation` section with apigee auto-detection |
| Persona detection | Basic EM/ADE | Adapts docs folder structure, common services population, BMAD config per persona |
| Multi-repo detection | Present | Enhanced with explicit BAIL when no git repos found |

### quick-setup — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Update mode | Not available | `update-workspace` mode re-scans repos, refreshes worker-repo registry, preserves existing config values |
| kb_generation config | Not present | Backfills `kb_generation` section (apigee detection mode) to existing i2a-config.yml during updates |
| Prompt count display | Static | Dynamic count display (33 prompts in sample output) |

### show-available-stories — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| GitHub MCP | Not required | Added prerequisite; falls back to git-only discovery if unavailable |
| Dependency evaluation | Present | Enhanced blocking condition detection across inter-story dependencies |

### update-context-docs — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Target role | Both (EM + ADE) | EM only — KB synchronization is an EM documentation responsibility |
| Branch scope | release/* only | Both release/* and project/* branches supported |
| Sync mode | Full reconciliation | Delta mode (commits since last sync) or full mode (missing metadata file) |
| Sync metadata | Not tracked | Records sync timestamp in metadata file for incremental detection |

### project-kanban-planning skill — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Location | `docs-github-starter/i2a-skills/` | `src/i2a-skills/` (restructured) |
| Update mode | Not available | Update mode reloads parameters from existing kanban-plan.md, skips to planning phase |
| Capacity model | ADE count only | Dual: ADE count OR target-date → derived ADE count |
| T-shirt sizing | Not available | Story estimation with T-shirt sizes (S/M/L/XL) and overhead factors |
| Dashboard delegation | Not present | Phase 4 delegates to new sprint-dashboard skill |

### generate-api-tests prompt — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Architecture | Fat prompt (all logic inline) | Thin orchestrator delegating to `generate-api-tests` skill |
| Discovery | Single-phase source code scan | Two-phase: KB/OpenAPI first, source code second |
| Quality gates | None | Pre-write contract (9 blocks), post-generation mechanical gate (20 checks) |
| Guardrails | None | 25 guardrails (G1-G25) for application-agnostic generation |
| Token resolution | Basic variable substitution | Catalog tokens (identityPool + stubs), captured tokens, field derivation hierarchy |

### setup-api-tests prompt — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Architecture | Fat prompt | Thin orchestrator delegating to `setup-api-tests` skill |
| Artifacts | Collections + environments | Full toolkit: test-runner.js, generate-report.js, lint-collection.js, audit-coverage.js, runner-contract.md |
| Output contract | Not defined | Pinned results.json schema, status enum, math invariant, exit codes |

### setup-unit-tests prompt — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Architecture | Fat prompt (all logic inline) | Thin orchestrator delegating to `setup-unit-tests` skill (8 files, 6 tool docs) |
| Stack detection | Basic framework detection | Router-style skill with dedicated scaffolds per stack (Java, JavaScript, Python, Angular, Vue, Lambda, .NET) |
| Guardrails | None | G1–G13 covering application-agnostic scaffolding, idempotency, and dashboard wiring |

### generate-unit-tests prompt — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Architecture | Fat prompt (all logic inline) | Thin orchestrator delegating to `generate-unit-tests` skill (10 files, 8 tool docs) |
| Quality gates | None | Pre-write contract (U1–U3), 8 guardrails (hermeticity, exception paths, exact assertions), 8 post-generation checks |
| Discovery | Single-phase source scan | Two-phase: KB discovery first, source-code discovery second |

### setup-functional-tests prompt — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Architecture | Fat prompt (all logic inline) | Thin orchestrator delegating to `setup-functional-tests` skill (10 files, 8 tool docs) |
| Artifacts | Basic Playwright scaffold | Full toolkit: page-object models, fixtures (api-mock, catalog, flow-runner), component helpers, flow descriptors |
| Guardrails | None | G1–G13 covering application-agnostic scaffolding, idempotency, and reports sync |

### generate-functional-tests prompt — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Architecture | Fat prompt (all logic inline) | Thin orchestrator delegating to `generate-functional-tests` skill (12 files, 10 tool docs) |
| Quality gates | None | Pre-write contract (F1–F6), gap analysis, 14+ post-gen checks + Standards Audit Script |
| Discovery | Single-phase flow scan | Multi-phase: flow descriptors, KB, source code |
| Execution modes | Single mode | Mock and real execution modes with flow × test-case × persona matrix |

### release.yml — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Version source | Auto-increment only (commit convention or manual dispatch) | Auto-increment with optional `NEXT_VERSION` file override; NEXT_VERSION must be valid semver and greater than auto-incremented value |
| NEXT_VERSION handling | Not present | Read NEXT_VERSION → validate format → compare against auto-increment → use if greater, warn and fall back if not → clear file in release commit |
| paths-ignore | VERSION, CHANGELOG.md, README.md, package.json, release.yml | Adds NEXT_VERSION to prevent release loops when clearing the file |
| Release commit files | VERSION, CHANGELOG.md, README.md, package.json | Adds NEXT_VERSION (cleared) to the git add list |
| Job summary | Previous/New version, bump type, tag | Adds Auto-Incremented, Released Version, and Version Source (auto or NEXT_VERSION) columns |

### Test Framework (Quality Infrastructure) — Enhanced

| Aspect | Master | Current |
|--------|--------|---------|
| Prompt count | 22 prompts validated | 33 prompts validated |
| Guide count | 3 monolithic guides | 14 topic guides |
| Skill validation | 1 skill (kanban-planning) | 11 skills with per-skill file inventory checks |
| Simulation rules | Present but smaller | 1987 lines, 126 functions, 39 categories covering all 33 prompts |
| Harness API | `pass`, `fail`, `skip`, `section` + file utilities | Adds `assertEqual`, `assertMatch`, `assertTrue` assertion helpers |
| Inventory suite | Checks prompt existence and naming | Also detects unexpected prompt files not in EXPECTED_PROMPTS |
| Cross-references suite | Validates anchor, file, prompt, and BMAD references | Also validates skill internal file references (SKILL.md → workflow.md, tools/*.md); unknown BMAD skills → skip for explicit triage |
| Version consistency suite | VERSION ↔ package.json ↔ README ↔ config alignment | Also validates NEXT_VERSION raw file format (no BOM, no extra whitespace) |
| Total checks | Not documented at master | 1,884 checks across 8 suites |
| test-bmad-upgrade | Present (standalone) | Removed (functionality merged into version consistency suite) |

### User Guides — Restructured with Quick-Access Features

| Aspect | Master | Current |
|--------|--------|---------|
| em-guide.md / ade-guide.md | Content-focused reading maps | Restructured with "Workflow at a Glance", "Quick Cheat Sheet", and "Verify Your Setup" sections for faster onboarding |
| prompt-reference.md | Flat command catalog | Added "What Do You Want to Do?" task-based lookup section — users find commands by goal, not by name |
| reference.md | Troubleshooting by individual symptom | Added "Common Issues — Quick Reference" summary table linking to detailed troubleshooting sections |

---

## Part 4 — Removed Capabilities

| Removed | Replacement | Notes |
|---------|-------------|-------|
| `tdgs-aidlc-switch-issue` | `tdgs-aidlc-switch` | Renamed; gains role-aware switching (EM → planning/*, ADE → dev/*) and direct spec-path argument |
| `agentic-user-guide/engineering-manager-user-guide.md` | `doc/em-guide.md` + 6 topic guides | Monolithic guide split into role hub + topic-based architecture |
| `agentic-user-guide/agentic-delivery-engineer-guide.md` | `doc/ade-guide.md` + 5 topic guides | Monolithic guide split into role hub + topic-based architecture |
| `agentic-user-guide/test-management-user-guide.md` | `doc/test-management.md` | Rewritten as topic guide with 3-layer pipeline, categorization matrix, operational rules |
| `docs-github-starter/` directory layout | `src/` directory layout | Repository restructured; prompts → `src/prompts/`, skills → `src/i2a-skills/`, config → `src/i2a-config.yml` |
| `docs-github-starter/.vscode/mcp.json` | `src/.vscode/mcp.json` | Relocated; Splunk MCP host changed from hardcoded to placeholder tokens |
| `test/test-bmad-upgrade.js` | Version consistency suite | Standalone BMAD version check merged into the existing `test-version-consistency.js` suite |

---

## Part 5 — Training Recommendations

### For Engineering Managers

#### Critical — Changes Daily Workflow

| # | Capability | Action | Impact |
|---|-----------|--------|--------|
| 1 | Sprint dashboard | Learn `/tdgs-aidlc-generate-dashboard` | Live HTML dashboard replaces manual status tracking; auto-refreshes from YAML |
| 2 | Metrics workflow | Learn `/tdgs-aidlc-update-metrics` + `/tdgs-aidlc-manage-blockers` + `/tdgs-aidlc-metrics-report` | Three new prompts form the daily sprint management loop: status → blockers → report |
| 3 | Course correction | Learn `/tdgs-aidlc-project-course-correction` | Formal mid-sprint change request workflow with impact analysis and artifact updates |
| 4 | Context switching | Migrate from the removed `switch-issue` command to `/tdgs-aidlc-switch` with `em` role | Old command removed; new command requires explicit role parameter for planning/* branches |
| 5 | Topic-based guides | Read `doc/em-guide.md` reading map | Monolithic guide replaced by topic-based system; reading map links to 8 relevant guides |

#### Important — New Capabilities to Adopt

| # | Capability | Action | Impact |
|---|-----------|--------|--------|
| 1 | Kanban planning prompt | Learn `/tdgs-aidlc-project-kanban-planning` | Orchestrates full sprint-ready planning with prerequisite detection and BMAD auto-delegation |
| 2 | Test data catalog | Learn `/tdgs-aidlc-setup-testdata` | Generates test-data-catalog.yaml with identity pools and API chains for API/functional tests |
| 3 | Test execution | Learn `/tdgs-aidlc-run-tests` | Consolidated test runner across all three layers with dashboard generation |
| 4 | Prompt reference | Bookmark `doc/prompt-reference.md` | Complete command reference for all 33 prompts and 11 skills |

#### Informational — Be Aware Of

| # | Capability | What Changed |
|---|-----------|-------------|
| 1 | Config preservation | `setup-workspace` now preserves existing i2a-config.yml instead of overwriting |
| 2 | GitHub MCP gate | `initiate-issue` and `initiate-project` now require GitHub MCP activation |
| 3 | Repository layout | Files moved from `docs-github-starter/` to `src/`; no user action needed (quick-setup handles installation) |
| 4 | Version override | Write a semver to `NEXT_VERSION` before merging to master to force a specific release version (e.g., 2.0.0 for a major release) |
| 5 | Guide quick-access | `em-guide.md` now includes "Workflow at a Glance", "Quick Cheat Sheet", and "Verify Your Setup" sections |
| 6 | Troubleshooting table | `reference.md` has a new "Common Issues — Quick Reference" summary table at the top of Troubleshooting |
| 7 | Prompt discovery | `prompt-reference.md` has a new "What Do You Want to Do?" task-based lookup section — find commands by goal |

### For Agentic Delivery Engineers

#### Critical — Changes Daily Workflow

| # | Capability | Action | Impact |
|---|-----------|--------|--------|
| 1 | Context switching | Migrate from the removed `switch-issue` command to `/tdgs-aidlc-switch` with `ade` role | Old command removed; new command requires explicit role parameter for dev/* branches |
| 2 | GitHub MCP prerequisite | Ensure GitHub MCP is activated before running `initiate-issue` | New mandatory gate; prompt BAILs if MCP unavailable |
| 3 | Topic-based guides | Read `doc/ade-guide.md` reading map | Monolithic guide replaced by topic-based system; reading map links to 6 relevant guides |
| 4 | Test skill pipelines | Learn `setup-{layer}` → `setup-testdata` → `generate-{layer}` skill chains for API, unit, and functional layers | All six test prompts now delegate to layered skills with contracts, guardrails, and quality gates |

#### Important — New Capabilities to Adopt

| # | Capability | Action | Impact |
|---|-----------|--------|--------|
| 1 | Test execution | Learn `/tdgs-aidlc-run-tests` | Single command executes unit + API + functional tests with consolidated reporting |
| 2 | Test data catalog | Learn `/tdgs-aidlc-setup-testdata` | Required before API/functional test generation; catalog tokens drive test data resolution |
| 3 | Story discovery | Learn enhanced `/tdgs-aidlc-show-available-stories` | Now evaluates inter-story dependencies for blocking conditions |
| 4 | MCP setup | Read `doc/mcp-setup-guide.md` | New guide covers GitHub, Splunk, and database MCP configurations |

#### Informational — Be Aware Of

| # | Capability | What Changed |
|---|-----------|-------------|
| 1 | Bug branches | `prepare-repos` now supports 3-tier bug branch derivation (project/epic/story level) |
| 2 | PR features | `create-pull-request` now auto-adds Copilot reviewer and comments on GitHub issue |
| 3 | Commit & repo discovery | `commit`, `pre-check-pull-request`, `switch`, `create-pull-request` now use i2a-config for repo discovery (includes symlinked common repos); expanded sensitivity checks |
| 4 | Quick-setup update mode | `quick-setup update-workspace` re-scans repos without altering existing config |
| 5 | Guide quick-access | `ade-guide.md` now includes "Workflow at a Glance", "Quick Cheat Sheet", and "Verify Your Setup" sections |
| 6 | Prompt discovery | `prompt-reference.md` has a new "What Do You Want to Do?" task-based lookup section — find commands by goal |

### For ACEs

#### Critical — Defines Workflow

| # | Capability | Action | Impact |
|---|-----------|--------|--------|
| 1 | Specification management | Learn `/i2a-manage-spec` (generate, update, validate, delta) | Authoritative spec documents are now code-generated from codebase scans; all spec updates go through this skill |
| 2 | Test suite management | Learn `/i2a-manage-test` | Test additions and modifications follow the documented skill workflow |
| 3 | Contributing guide | Read `doc/contributing/README.md` | Central development procedures, commit conventions, release workflow |
| 4 | Catalog maintenance | Read and maintain `doc/contributing/catalog.md` | Dependency graph, upgrade impact matrix, config-consumer map — must be updated when adding prompts/skills |

#### Important — Foundational Knowledge

| # | Capability | Action | Impact |
|---|-----------|--------|--------|
| 1 | Adversarial review | Learn `/i2a-manage-review` | 6-persona review skill catches cross-reference errors, content gaps, and consistency issues before release |
| 2 | Functional specification | Read `doc/contributing/functional-specification.md` | 56 FRs + 9 NFRs define the capability contract (spec v1.7.1); update via `/i2a-manage-spec update` |
| 3 | Technical specification | Read `doc/contributing/technical-specification.md` | Architecture contract with extension guides for adding prompts, skills, guides, test suites, and simulation rules |
| 4 | Simulation rules | Study `test/simulation/rules.js` (1987 lines, 126 functions) | Every new prompt with deterministic logic needs extracted rules and test cases |

#### Informational — Reference Material

| # | Capability | What Changed |
|---|-----------|-------------|
| 1 | Repository layout | `docs-github-starter/` → `src/`; all test paths updated accordingly |
| 2 | Test check count | 1,884 total checks across 8 suites (up from master baseline); includes NEXT_VERSION validation, unexpected prompt detection, skill internal file references |
| 3 | Skill file inventory | Test inventory validates per-skill required files for all 11 skills (SKILL.md, workflow.md, templates/, tools/) |
| 4 | Delta documents | `/i2a-manage-spec delta` and `/i2a-manage-spec delta update` modes generate this training document |
| 5 | NEXT_VERSION pipeline | Release workflow reads `NEXT_VERSION` file, validates format and magnitude, uses it if greater than auto-increment, clears it in the release commit; NFR-9 documents the capability contract |

---

*Generated by `i2a-manage-spec delta` — content comparison of original master baseline vs v2.1.1*
